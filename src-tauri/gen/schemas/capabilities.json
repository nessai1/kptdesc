{"default":{"identifier":"default","description":"Permissions for the diary window. Files and the report are handled by Rust commands, so the WebView needs no filesystem or shell access of its own. Closing is intercepted to flush pending saves, which needs explicit permission to destroy the window afterwards.","local":true,"windows":["main"],"permissions":["core:default","core:window:allow-destroy"]}}